<h1><%= @articles.title.html_safe %></h1>
<p> <%= @articles.body %></p>

<%= link_to "back",root_path %>

<%= link_to "delete", article_path(@articles),
           method: :delete,
           data: { confirm: "delete it" }
%>

