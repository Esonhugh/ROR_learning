<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
      <iframe srcdoc="<p><%= article.body.html_safe %></p>">
      </iframe>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>